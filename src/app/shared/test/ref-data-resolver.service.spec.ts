/* tslint:disable:no-unused-variable */
/*
import { TestBed, async, inject } from '@angular/core/testing';
import { RefDataResolverService } from './ref-data-resolver.service';

describe('RefDataResolverService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [RefDataResolverService]
    });
  });

  it('should ...', inject([RefDataResolverService], (service: RefDataResolverService) => {
    expect(service).toBeTruthy();
  }));
});*/
